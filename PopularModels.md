# 🧠 대표적인 딥러닝 모델들 소개 (기초 설명)



## 🔧 딥러닝 모델의 기본 구조 3가지


### 1️⃣ FC (Fully Connected, 완전 연결층)

- 가장 기본적인 구조
- 여러개의 입력값과 여러개의 출력값이 있음.
- 이때 각 출력값은 모든 입력값과 연결됨


```
입력(숫자들) → [ 모든 입력값과 가중치를 적용하여 연결 ] → 출력
```


</br>
</br>

### 2️⃣ CNN (Convolutional Neural Network, 합성곱 신경망)

- **이미지 분석**에 특화된 구조
- 작은 필터(커널)가 이미지를 훑으며 특징을 추출함
- 특징: **공간적 구조(가로×세로)를 유지**하며 학습

**쉽게 비유**

- 이미지는 보통 RGB 채널로 되어 있고 \[가로, 세로\]의 픽셀값으로 구성되어 있음.
- 직관적으로 보면, 이미지 위에서 작은 정사각형 색종이를 쭉 움직여보면서, 색종이가 있는 부분의 특징을 추출해서 출력값을 계산함.
- 출력값도 \[가로, 세로\]의 픽셀 형태를 가지고 있으며, 이때 출력 사이즈는 입력 사이즈와 달라질 수 있음.



</br>
</br>

### 3️⃣ RNN (Recurrent Neural Network, 순환 신경망)

- **시간 순서**가 있는 데이터를 처리
- 앞에서 받은 정보를 기억해서 다음 단계에 반영
- 특징: **문장, 소리, 시계열 데이터**에 적합

```
입력 t1 ──▶ RNN 셀 ──┐
                     ↓
       입력 t2 ──▶ RNN 셀 ──┐
                             ↓
              입력 t3 ──▶ RNN 셀 ──▶ ...
```

📌 예: 문장이나 글 생성, 주가 예측


</br>

---


## 🔧 딥러닝에서 사용되는 특수한 함수나 기능들


</br>

## ⚡ ReLU (Rectified Linear Unit)

> ❓ "복잡한 계산을 하기 전에, 간단하게 통과할지 말지를 결정해요!"

### ✅ 개념
- 입력값이 **0보다 크면 그대로 통과**, 아니면 0으로 바꿔줍니다.
- 음수는 버리고, 양수는 유지합니다.

### ✅ 왜 쓰나요?
- 연산을 단순하게 만들어서 학습을 빠르게 도와줍니다.
- 복잡한 함수보다 훨씬 빠르게 작동합니다.

```
입력: -3 → ReLU → 0  
입력: 5  → ReLU → 5
```

📌 직관: **"양수만 통과하는 거름망"**


</br>
</br>

## 🕳️ Dropout

> ❓ "학습할 때 일부 뉴런을 **일부러 무시**해요!"

### ✅ 개념
- 학습 중 일부 뉴런을 **무작위로 꺼서 사용 안 함**
- 훈련할 때만 적용되고, 실제 예측 시에는 **전부 사용**

### ✅ 왜 쓰나요?
- 모델이 한 뉴런에 **너무 의존하지 않도록**
- 다양한 뉴런이 **고르게 학습**되도록
- **과적합(overfitting)** 방지

```
훈련할 때:
[O] [X] [O] [X] → 일부 꺼짐

예측할 때:
[O] [O] [O] [O] → 모두 사용
```

</br>
</br>

---

## 🏛️ 대표적인 딥러닝 모델

---

### 🖼️ [LeNet-5] - 이미지 숫자 인식

- 최초의 CNN 기반 딥러닝 모델
- 손글씨 숫자(MNIST) 분류용
- 구조: Conv → Pool → FC

📌 "딥러닝"의 시초

</br>

### 📷 [AlexNet] - 이미지 분류 혁명

- ImageNet 대회 우승으로 딥러닝 붐 촉발
- GPU로 학습
- 구조: 더 깊어진 Conv + ReLU + Dropout

📌 CNN의 가능성을 전 세계에 알림


</br>

### 🔁 [LSTM] - 긴 문장 기억 가능

- RNN의 단점(기억 소실)을 해결한 구조
- 중요한 정보를 오래 기억 가능

📌 챗봇, 번역, 음성 인식 등에서 사용


</br>

### 🏗️ [ResNet] - 아주 깊은 네트워크 가능

- 152층까지 가능해짐 (이전엔 20~30층도 힘들었음)
- 핵심: **잔차 연결(skip connection)**

📌 깊은 CNN 학습을 안정화시킨 모델

</br>

### 🧠 [Transformer] - 요즘 딥러닝의 중심

- 문장 전체를 한 번에 처리
- 기존 RNN보다 빠르고 정확
- 대표 구조: Self-Attention

📌 GPT, BERT, ChatGPT 등은 모두 이 구조 기반


