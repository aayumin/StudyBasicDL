# 🤖 딥러닝/머신러닝 입문 자료

---

## 📌 인공지능(AI), 머신러닝(ML), 딥러닝(DL)의 관계

- **인공지능 (AI)**  
  : 사람처럼 생각하고 행동하는 컴퓨터 프로그램 전반  
  → "기계가 인간처럼 행동하도록 만드는 기술"

- **머신러닝 (ML)**  
  : AI의 하위 개념. 데이터를 통해 **스스로 학습**하는 기술  
  → "프로그래밍 없이 학습하는 알고리즘"

- **딥러닝 (DL)**  
  : 머신러닝의 하위 개념. **인공 신경망**을 활용한 학습 방법  
  → "뇌처럼 작동하는 구조(신경망)를 이용한 머신러닝"

> 관계 요약:  
> **AI > 머신러닝 > 딥러닝**  
> (AI가 가장 큰 범주. 그 안에 머신러닝이 있고, 그 안에 딥러닝이 있음.)

</br>

## 📂 데이터란?

- **데이터**는 **기계가 학습하기 위한 재료**
- 보통은 **표 (2차원 테이블)** 형태로 구성됨

| 나이 | 키(cm) | 몸무게(kg) | 운동 여부 |
|------|--------|------------|------------|
| 25   | 170    | 65         | O          |
| 30   | 165    | 70         | X          |
| ...  | ...    | ...        | ...        |

- 각 **행(row)**: 하나의 샘플 (사람 1명 정보 등)  
- 각 **열(column)**: 특징(feature) (나이, 키, 몸무게 등)

---

## 🎯 머신러닝 문제 유형

### 1. 회귀 (Regression)

- 숫자 값을 예측하는 문제
- 예: 집 값 예측, 온도 예측, 키 예측

### 2. 분류 (Classification)

- 범주(클래스)를 구분하는 문제
- 예: 스팸 메일 vs 정상 메일, 암 진단(양성/음성)

</br>


## 📈 대표적인 머신러닝 알고리즘

### 🔹 선형 회귀 (Linear Regression)

- 하나의 직선으로 데이터를 설명하는 방법
- 예:  
  - 입력: 공부 시간  
  - 출력: 시험 점수  
  - 회귀선: `점수 = 5 × 공부시간 + 30`

### 🔹 결정 트리 (Decision Tree)

- 질문을 분기하면서 판단하는 트리 구조
- 예:  
  ```
  운동 여부는?
    ├─ O → 건강할 확률 높음
    └─ X → 건강할 확률 낮음
  ```

---

## 🧠 딥러닝(Deep Learning)이란?

- 여러 층으로 겹겹이 쌓인 **인공 신경망(Neural Network)** 기반의 학습
- 실제 인간 뇌의 구조를 모방함
- 인간의 뇌는 수많은 **퍼셉트론(Perceptron)** 으로 구성되어 있음.


</br>


## 🧩 퍼셉트론 (Perceptron) - 뇌의 뉴런을 닮은 계산 구조

**신경세포(뉴런)**

- 인간의 뇌에는 신경세포(뉴런)가 있고 각 신경세포들 사이에 전기신호를 통해 정보를 주고 받음.
- 하나의 신경세포(뉴런)에는 여러개의 입력값과 하나의 출력값이 있음.
- 예를 들어 우리가 사과를 보면, 시각적인 정보가 전기신호를 통해 신경세포(뉴런)에 입력되고, 정보량이 일정 수준 이상이면 신경세포가 활성화 되면서, 전기신호를 통해 어떤 정보를 출력하게 됨.

**실제로 컴퓨터로 신경세포(뉴런)를 구현하는 방법.**

- 입력값은 x1, x2, x3 이고 출력값은 y 일 때, `y = x1 * 0.1 + x2 * 0.7 + x3 * 0.4` 으로 계산되는 신경세포가 있다고 가정하면,
- 만약 사과를 봤을 때 `x1 = 3, x2 = 1, x3= -5` 라면 `y = -1` 이라는 출력값이 나오게 됨.
- 그리고 **활성화 함수 (Activation Function)** 라는 게 있어서, 출력값이 일정 수준 이상이어야만 실제로 전기신호가 출력됨.
- 예를 들면 `y > 0` 일 때만 실제로 출력되는 활성화 함수를 가지고 있다면, 위의 예시처럼 y = -1 일 때는 아무 신호도 출력되지 않음.


</br>


## 🏗️ MLP (다층 퍼셉트론, Multi-Layer Perceptron)

- 퍼셉트론을 여러 층으로 쌓은 구조
- 구성:
  - 입력층 (input layer)
  - 은닉층 (hidden layers) → 이 부분에서 몇 겹이든 원하는 대로 레이어를 쌓을 수 있음. 함수처럼 내부에서 계산되는 곳.
  - 출력층 (output layer)

- 딥러닝 모델의 기본 뼈대

---

## 📉 Loss Function (손실 함수)

- 모델의 예측이 **정답과 얼마나 다른지** 계산, 즉 오차를 계산함.
- 값이 작을수록 정확한 예측을 한 것
- 예를 들면, MSE (Mean Squared Error) = 각 케이스별로 (예측값 - 정답) 의 제곱을 계산하여 평균내기.


</br>

## 🔁 Backpropagation (역전파)

- 딥러닝 모델을 학습하는 핵심적인 원리임.
- 입력값은 x1, x2, x3 이고 출력값은 y 일 때, `y = x1 * 0.1 + x2 * 0.7 + x3 * 0.4` 으로 계산되는 신경세포가 있다고 가정하면, 여기서 실제 데이터를 보고 `0.1, 0.7, 0.4` 등의 값을 조정하면서 가장 최적의 값을 찾는 것이 딥러닝 모델의 학습 방법임.

  
- 이때 데이터를 실제로 함수처럼 계산해보고, **오차에 대한 정보를 출력에서부터 입력 방향으로 전달**하며 각각의 가중치(ex: 0.1, 0.7, 0.4 등의 값)를 조정
- 실제 데이터를 정답에 가깝게 잘 예측할 수 있도록, 오차가 작아지는 방향으로 가중치를 업데이트


</br>

## 🧠 딥러닝 모델을 학습한다는 것

- 딥러닝 모델은 수많은 퍼셉트론으로 이루어짐.
- 그 중에서 하나의 퍼셉트론이 `y = x1 * 0.1 + x2 * 0.7 + x3 * 0.4` 으로 계산된다고 하면, 여기서 `0.1, 0.7, 0.4` 등의 값을 `가중치(weight), 파라미터(parameter)` 라고 한다.
- 실제 데이터를 통해 우리는 \[x1, x2, x3, y\] 를 대략 알고 있으며, 이 함수 관계를 만족하도록 `0.1, 0.7, 0.4` 를 세밀하게 조정하는 것.
- 그리고 전반적으로 퍼셉트론이 배치된 구조와 `0.1, 0.7, 0.4` 등의 값들을 모두 합쳐서 `모델(model)`이라고 한다.
- 학습되기 전의 모델은 `0.1, 0.7, 0.4` 등의 값이 랜덤하게 설정되어 있고, 학습된 모델은 `0.1, 0.7, 0.4` 등의 값이 데이터를 통해 세밀하게 조정되어 있음.


---

