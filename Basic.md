# 🤖 딥러닝/머신러닝 입문 자료 (초보자용)

---

## 📌 인공지능(AI), 머신러닝(ML), 딥러닝(DL)의 관계

- **인공지능 (AI)**  
  : 사람처럼 생각하고 행동하는 컴퓨터 프로그램 전반  
  → "기계가 인간처럼 행동하도록 만드는 기술"

- **머신러닝 (ML)**  
  : AI의 하위 개념. 데이터를 통해 **스스로 학습**하는 기술  
  → "프로그래밍 없이 학습하는 알고리즘"

- **딥러닝 (DL)**  
  : 머신러닝의 하위 개념. **인공 신경망**을 활용한 학습 방법  
  → "뇌처럼 작동하는 구조(신경망)를 이용한 머신러닝"

> 관계 요약:  
> **AI > 머신러닝 > 딥러닝**  
> (딥러닝은 머신러닝의 일종, 머신러닝은 AI의 일종)

---

## 📂 데이터란?

- **데이터**는 **기계가 학습하기 위한 재료**
- 보통은 **표 (2차원 테이블)** 형태로 구성됨

| 나이 | 키(cm) | 몸무게(kg) | 운동 여부 |
|------|--------|------------|------------|
| 25   | 170    | 65         | O          |
| 30   | 165    | 70         | X          |
| ...  | ...    | ...        | ...        |

- 각 **행(row)**: 하나의 샘플 (사람 1명 정보 등)  
- 각 **열(column)**: 특징(feature) (나이, 키, 몸무게 등)

---

## 🎯 머신러닝 문제 유형

### 1. 회귀 (Regression)

- 숫자 값을 예측하는 문제
- 예: 집 값 예측, 온도 예측, 키 예측

### 2. 분류 (Classification)

- 범주(클래스)를 구분하는 문제
- 예: 스팸 메일 vs 정상 메일, 암 진단(양성/음성)

---

## 📈 대표적인 머신러닝 알고리즘

### 🔹 선형 회귀 (Linear Regression)

- 하나의 직선으로 데이터를 설명하는 방법
- 예:  
  - 입력: 공부 시간  
  - 출력: 시험 점수  
  - 회귀선: `점수 = 5 × 공부시간 + 30`

### 🔹 결정 트리 (Decision Tree)

- 질문을 분기하면서 판단하는 트리 구조
- 예:  
  ```
  운동 여부는?
    ├─ O → 건강할 확률 높음
    └─ X → 건강할 확률 낮음
  ```

---

## 🧠 딥러닝(Deep Learning)이란?

- 여러 층으로 쌓인 **인공 신경망(Neural Network)** 기반의 학습
- 뇌의 뉴런 구조를 본뜬 **퍼셉트론(Perceptron)** 이 핵심

---

## 🧩 퍼셉트론 (Perceptron)

- 입력값 여러 개 → 가중치 곱 → 합산 → 활성화 함수 → 결과
- 간단한 구조의 인공 뉴런

```
입력 X1 ─┬─> 가중치 W1 ─┐
입력 X2 ─┤               ├─> 합계 → 활성화 → 출력
입력 X3 ─┘─> 가중치 W3 ─┘
```

---

## 🏗️ MLP (다층 퍼셉트론, Multi-Layer Perceptron)

- 퍼셉트론을 여러 층으로 쌓은 구조
- 구성:
  - 입력층 (input layer)
  - 은닉층 (hidden layers) → 비선형 처리
  - 출력층 (output layer)

- 딥러닝 모델의 기본 뼈대

---

## 📉 Loss Function (손실 함수)

- 모델의 예측이 **정답과 얼마나 다른지** 계산
- 값이 작을수록 정확한 예측을 한 것
- 예:
  - 회귀: MSE (Mean Squared Error)
  - 분류: Cross Entropy Loss

---

## 🔁 Backpropagation (역전파)

- 오차를 **출력 → 입력 방향으로 전달**하며 각 가중치를 조정
- 손실이 작아지도록 파라미터를 업데이트
- 학습의 핵심 과정

---

## 📚 요약 정리

| 개념 | 설명 |
|------|------|
| AI | 인간처럼 생각·행동하는 기술 |
| ML | 데이터를 통해 기계가 스스로 학습 |
| DL | 신경망을 사용하는 ML |
| 데이터 | 학습의 재료 (2D 테이블 형태) |
| 회귀 | 숫자를 예측 |
| 분류 | 범주를 예측 |
| 선형회귀 | 직선 모델 |
| 결정트리 | 질문을 따라가며 예측 |
| 퍼셉트론 | 인공 뉴런 |
| MLP | 여러 퍼셉트론 층 |
| 손실 함수 | 예측이 얼마나 틀렸는지 측정 |
| 역전파 | 오차를 따라 가중치 업데이트 |

---

## ✅ 추천 학습 순서

1. 위 개념 숙지
2. scikit-learn, TensorFlow, PyTorch 중 택1 → 실습 시작
3. 예제 따라하며 구현 경험 쌓기
4. 간단한 프로젝트 시도 (예: 영화 리뷰 감정 분류)

---

💡 *너무 깊게 들어가기보다, 먼저 큰 흐름을 이해하는 것이 가장 중요합니다!*
